# Control Flow

## ForEach

```bella
 foreach <Type | Alias> in <ICollection>
        <do something about it>
```

## If

```bella
if <condition>
    <do something about it>
else if
    <do something about it>
else
    <>
// used in formulas
if(<condition>, <ifTrue>, <ifFalse>)
```
