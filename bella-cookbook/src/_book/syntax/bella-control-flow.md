# Control Flow

## ForEach

Example:

```bella
 foreach <Type | Alias> in <ICollection>
        <do something about it>
```

## If

Example:

```bella
if <condition>
    <do something about it>
else if
    <do something about it>
else
    <>
// used in formulas
if(<condition>, <ifTrue>, <ifFalse>)
```
